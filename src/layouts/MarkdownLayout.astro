---
import MainGridLayout from '$/layouts/MainGridLayout.astro'
import Markdown from '$/components/post/Markdown.astro'

interface Props {
  frontmatter: Readonly<{
    title: string
    description?: string | undefined
  }>
}

const { title, description = title } = Astro.props.frontmatter

// Astro doesn't validate prop types in Markdown layouts, so we've gotta do it
// manually.
if (typeof title !== 'string') {
  throw new TypeError(
    `invalid \`title\` prop (type \`${typeof title}\` is not assignable to \`string\`)`,
  )
}
if (typeof description !== 'string') {
  throw new TypeError(
    `invalid \`description\` prop (type \`${typeof description}\` is not assignable to \`string | undefined\`)`,
  )
}
---

<MainGridLayout {title} {description}>
  <div class="outer-wrapper">
    <div class="inner-wrapper">
      <Markdown class="mt-2">
        <slot />
      </Markdown>
    </div>
  </div>
</MainGridLayout>

<style lang="scss">
  @use '$/styles/main';
  @use '$/styles/theme' as *;
  @use '$/styles/utils' as *;
  @use '$/styles/variants';

  .outer-wrapper {
    display: flex;
    width: 100%;
    border-radius: $radius-xl2;
    overflow: hidden;
    position: relative;
    min-height: spacing(32);

    .inner-wrapper {
      @extend %card-base;
      z-index: 10;
      padding: spacing(6) spacing(9);
      position: relative;
      width: 100%;
    }
  }
</style>
