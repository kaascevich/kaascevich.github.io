<div class="back-button">
  <button onclick="history.length === 1 ? location = '/' : history.back();">
    go back
  </button>
</div>

<style lang="sass">
  button
    +hover-transition($opacity: 75%)
    @include hide-in-print
    &::before
      content: chars.$arrow-left
</style>
