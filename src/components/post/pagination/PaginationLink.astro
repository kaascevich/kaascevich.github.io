---
import type { Post } from '$/types/content'

import { getPostUrlByID } from '$/utils/urls'
import { Icon } from 'astro-icon/components'

interface Props {
  entry: Post
  type: 'next' | 'prev'
}

const { entry, type } = Astro.props

const icon = `tabler:chevron-${type === 'next' ? 'left' : 'right'}` as const
---

<a href={getPostUrlByID(entry.id)} class={type}>
  <div class="link-wrapper">
    <Icon name={icon} />
    <div class="title">{entry.data.title}</div>
  </div>
</a>

<style lang="scss">
  @use '$/styles/main';
  @use '$/styles/theme' as *;
  @use '$/styles/utils' as *;
  @use '$/styles/variants';

  a {
    width: 100%;
    max-width: 100%;
    overflow: hidden;

    font-weight: $font-weight-bold;

    @include variants.md {
      max-width: calc(50% - spacing(2));
    }

    &:active {
      scale: 95%;
    }

    .link-wrapper {
      @extend %btn-card;

      display: flex;
      align-items: center;
      justify-content: flex-start;
      width: 100%;
      max-width: 100%;
      height: spacing(15);
      padding-right: spacing(4);
      padding-left: spacing(4);

      border-radius: $radius-xl2;
      gap: spacing(4);

      .title {
        @include transition;
        @include text-plain(75%);

        max-width: calc(100% - spacing(12));

        overflow: hidden;

        text-overflow: ellipsis;
        white-space: nowrap;
      }

      svg {
        width: spacing(8);
        height: spacing(8);

        color: var(--primary);
      }
    }

    &.next .link-wrapper {
      flex-direction: row;
    }

    &.prev .link-wrapper {
      flex-direction: row-reverse;
    }
  }
</style>
