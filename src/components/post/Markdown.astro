---
// import CopyButtonScript from '$/components/scripts/CopyButtonScript.svelte'
// import '$/scripts/init/copyButtons'

export interface Props {
  class?: string
}
const { class: className } = Astro.props
---

<article
  data-pagefind-body
  class:list={['prose prose-base dark:prose-invert', className]}
>
  <slot />
</article>

<!-- <CopyButtonScript client:load /> -->
<script src="$/scripts/init/copyButtons"></script>

<style lang="scss">
  @use '$/styles/main';
  @use '$/styles/theme' as *;
  @use '$/styles/utils' as *;
  @use '$/styles/variants' as *;

  article {
    max-inline-size: none !important;
  }

  :global(.code-block) {
    position: relative;
  }
</style>
