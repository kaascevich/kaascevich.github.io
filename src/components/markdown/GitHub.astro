---
import GitHubSvelte from '$/components/markdown/_GitHub.svelte'

type Props = Readonly<{
  /** A GitHub repository, in the form "owner/repo". */
  repo: `${string}/${string}`
}>
const { repo } = Astro.props
---

<GitHubSvelte client:visible {repo} />
